<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ext_result_mng">


	<!-- 공통 코드 목록 조회 -->
	<select id="selectCommonCodeList" resultType="sangsMap" parameterType="sangsMap">
		select 
			cd as code
			, cd_nm as code_nm
		from EGOV_CMNCDDTLCODE
		where cd_id = #{groupCmmnCd}
			and use_yn = 'Y'
		order by sort_no
	</select>


	<!-- 다중 공통 코드 목록 조회 -->
	<select id="selectMultiCommonCodeList" resultType="sangsMap" parameterType="java.util.ArrayList">
		select
			cd_id as group_cmmn_cd
			, cd as code
			, cd_nm as code_nm
		from EGOV_CMNCDDTLCODE
		where use_yn = 'Y'
			and cd_id in (
			<foreach collection="list" separator="," item="item" >
				#{item}
			</foreach>
			)
		order by cd_id, sort_no
	</select>
	 
	 
 
	
</mapper>  