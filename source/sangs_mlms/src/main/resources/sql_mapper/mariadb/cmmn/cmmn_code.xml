<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cmmn_code">


	<!-- 공통 코드 목록 조회 -->
	<select id="selectCommonCodeList" resultType="sangsMap" parameterType="sangsMap">
		select 
			cmmn_cd as code
			, cmmn_cd_nm as code_nm
		from CMMN_CODE
		where group_cmmn_cd = #{groupCmmnCd}
			and del_yn = 'N'
		order by sort_no
	</select>


	<!-- 다중 공통 코드 목록 조회 -->
	<select id="selectMultiCommonCodeList" resultType="sangsMap" parameterType="java.util.ArrayList">
		select
			group_cmmn_cd
			, cmmn_cd as code
			, cmmn_cd_nm as code_nm
		from CMMN_CODE
		where del_yn = 'N'
			and group_cmmn_cd in (
			<foreach collection="list" separator="," item="item" >
				#{item}
			</foreach>
			)
		order by group_cmmn_cd, sort_no
	</select>
	 
	 
 
	
</mapper>  