<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mlms_code">

 
	<!-- 학습그룹 목록 조회 -->
	<select id="selectLearningGroupList" resultType="sangsMap" parameterType="sangsMap">
		select
			lrn_group_sn as code
			, lrn_group_nm as code_nm
		from ML_LRN_GROUP
		where del_yn ='N'
		order by lrn_group_sn
	</select>
	
 	<!-- 학습그룹 목록 순번 채번 -->
 	<select id="selectNextLearningGroupSn" resultType="int" parameterType="sangsMap">
 		select
			ifnull(max(lrn_group_sn), 0) + 1 as nextLrnGroupSn
		from ML_LRN_GROUP
 	</select>
	
</mapper>  